# Tests

- [tests/vocabulary.test.ts](../tests/vocabulary.test.ts) — `scoreWord`, `computeSelectedVocabulary`, `getSelectedVocabulary` (including property-based with fast-check).
- [tests/llm.test.ts](../tests/llm.test.ts) — `parseMisusedWords`, `stripMisusedJson` (no live API calls).

Run with `npm run test`.
